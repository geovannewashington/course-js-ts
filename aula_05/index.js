let nomeVariavel = 30; 
console.log(nomeVariavel);
console.log(nomeVariavel);
console.log(nomeVariavel);
// Não pode conter caracteres especiais, ex: $nomeVariável
// Não pode começar por números, ex: 2nomeVariavel
// Não pode conter espaços, ex: nome varial
// Não pode ser o mesmo nome de palavras já reservadas pelas linguagem, ex: const
// Não utilize var, utilize let
// Não podemos redeclarar variáveis com let

nomeVariavel = 20;